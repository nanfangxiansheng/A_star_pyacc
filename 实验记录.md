# 主干实验

主干实验的目的是进行三维路径寻找对比，这里进行记录，测试十次。每次的条件是同一设定的基本的大障碍物以及随机生成的5个小障碍物.主干实验进行对比的算法包括：采用二次加速的A *算法，原始版本的A *算法，RRT\*算法，RRT算法。每次的起点和终点都在规划的三维空间的(0,0,0)和(MAX_X,MAX_Y,MAX_Z)周围加上随机的偏移。在测试过程中，MAX_X,MAX_Y,MAX_Z的值均设置为50，在这种情况下，计算复杂度较高。

在测试十次得到的结果如下：

双重优化算法的平均耗时为0.0172s,原始算法的平均耗时为24.892s。RRT star算法的平均耗时为0.2671s, RRT算法的平均耗时为5.47s。

在规划的路径长度这个指标上，双重优化算法的平均路径长度为77.64，原始的A star算法的平均路径长度为79.34.RRT star算法的平均路径长度为85.18，RRT原始算法的平均路径长度为188.08.

# 消融部分实验

消融部分实验需要对双重加速的若干组件进行剥离，看各自对于加速效果的贡献。在加速过程中用到了预先计算邻近值和数据结构优化的第一重加速以及使用加权启发函数的第二重优化。随后还需要对加权形式的启发函数的权重进行消融实验。

## 加速模块的消融实验

首先对第一重加速中的数据结构优化以及预先计算到邻近节点距离的优化进行消融实验。在平均测试十次之后，对比使用了预先计算到邻近节点距离和没有预先计算到邻近节点距离的A star算法的结果。以及对比仅仅使用了数据结构优化和没有使用数据结构优化的结果。

原始算法耗费的时间为23.764s，预先计算到邻近节点距离平均耗时为23.126s。而使用了数据结构优化后的平均耗时仅仅为0.645s。这也证实了数以万计的在数组中最小元素检索是决定搜索过程耗时的主要部分，原始的检索时间复杂度是O(n)，而采用了基于二叉树堆的数据结构优化后的检索时间复杂度是O(logn)。因此可见结果是数据结构优化后的搜索时间以数量级的幅度下降。

其次对第二重加速中的使用加权启发函数和原始启发函数的结果进行对比，发现在使用加权启发函数（权重w值设置为1.2）后，检索的时间耗费再次以数量级下降。不使用加权启发函数但是使用了前面提到的第一重加速后的平均耗时为0.5849s，使用了两重加速后的平均耗时为0.0087s。

## 加权启发函数权重的消融实验

在平均测试十次之后，记录得到数据如下：

在w的值为1.0的时候，记录得到的平均时间为0.379s.w的值为1.05的时候，记录得到的平均时间为0.079s.在w=1.1s的时候，记录得到的平均时间为0.014s，在w的值为1.2的时候，记录得到的平均时间为0.0036s。在w的值为1.3的时候，记录得到的平均时间为0.0018s，在w的值为1.5的时候，记录得到的平均时间为0.0014s，在w的值为2.0的时候，记录得到的平均时间为0.0020s。

而在求解得到的平均路径长度中从w值为1.0的时候，测得的平均路径长度为79.34，而到了w值为2.0的时候，测得的平均路径长度为79.82。

综上可知，选择w为1.2是一个可以被接收的折中方案，这使得路径搜索速度较快的同时还能保持平均路径长度较短。并且在w值逐渐增加的过程中，还面临着问题如在复杂地形中更容易陷入局部最优或死胡同，找到的路径不是最优路径等。

